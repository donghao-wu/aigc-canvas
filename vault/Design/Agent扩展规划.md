# Agent 扩展规划

> 当前 6 个 agent 覆盖剧本生产。下面是下一步可以扩展的 agent 方向。

## 优先级评估

| Agent | 价值 | 实现难度 | 优先级 |
|-------|------|---------|--------|
| 审稿 Agent | 高 | 低 | P0 |
| Character DNA | 高 | 低 | P0 |
| 一致性检查 Agent | 高 | 中 | P1 |
| 美术指导 Agent | 中 | 中 | P1 |
| 投流文案 Agent | 高 | 低 | P1 |
| 分镜 Agent | 中 | 高 | P2 |

---

## P0：审稿 Agent

**输入**：storyBible + characterBios + episodeContent（指定集数）
**输出**：结构化审稿报告

```
审稿维度：
- 节奏：开场钩子是否够强？集尾悬念是否有力？
- 人设：角色行为是否符合小传中的性格？有无OOC（出戏）？
- 格式：△行比例是否合规（<14%）？对白行格式是否正确？
- 爽感：这集有没有爽点？爽点够不够给力？
- 对白：台词是否口语化？有无书面语堆砌？
输出格式：每个维度给出评分（1-5）+ 具体问题描述 + 修改建议
```

**开发建议**：
- system prompt 大量引用逐集剧本 prompt 的规则作为评分标准
- 可以复用现有 `/api/script-agent` 接口，加一个 `mode: review_episode`

---

## P0：Character DNA

**作用**：给每个角色生成一段固定英文"锚定描述"，所有涉及该角色的生图提示词都拼接这段 DNA，确保跨图一致性。

**格式示例**：
```
林若曦-DNA: 25-year-old Chinese woman, slim build, long straight black hair, cold sharp eyes, usually wearing white blazer or professional attire, tall posture
```

**实现方案**：
- 在资产登记 prompt 中加入 `DNA:` 字段（独立于 `提示词-三视图` 等）
- 前端展示 DNA 字段，支持复制
- 推送到画布时，每个节点的 presetPrompt = DNA + 角度模板

**与现有资产登记的区别**：
- `外形` 字段：中文，给人类读的
- `提示词-三视图`：完整提示词，可直接粘贴到 NanoBanana
- `DNA`：英文关键描述，用于拼接到其他提示词（用于一致性）

---

## P1：投流文案 Agent

**输入**：storyBible + episodeMap（第1-3集）
**输出**：5条不同风格的短视频投流文案（抖音/快手风格）

```
输出格式：
版本1（悬疑钩）：[15字以内标题] + [30字以内描述]
版本2（爽感钩）：...
版本3（情感钩）：...
版本4（身份钩）：...
版本5（反转钩）：...
```

价值：投流文案是短剧变现的关键，目前完全手工，自动化空间大。

---

## P1：美术指导 Agent

**输入**：storyBible + styleConfig + 已生成的部分资产图（base64）
**输出**：视觉一致性评估 + 调整建议

```
评估维度：
- 角色间色调是否统一
- 场景光线是否一致
- 画风是否与选定 styleConfig 吻合
- 提示词优化建议
```

---

## P2：分镜 Agent

**输入**：episodeContent（单集剧本）+ assetRegistry
**输出**：分镜脚本（每个场景 → 多个镜头描述 + 对应资产引用）

```
格式：
场景 1-1
  镜头1：[景别] [角度] [主体] [动作] → 资产: 场景-全景
  镜头2：[景别] [角度] [主体] [动作] → 资产: 人物-特写
  ...
```

**难点**：需要理解分镜语言，且输出要能直接驱动 Canvas 的节点布局。

---

## 多 Agent 协作架构（未来）

```
剧本组工作流：
用户 → 故事圣经 → 角色小传 → 资产登记 → 集数大纲 → 逐集剧本 → 审稿
                                                                    ↓
                                                              （修改建议）
                                                                    ↓
                                                              逐集剧本（修改版）

美术组工作流：
资产登记 → 推送画布 → NanoBanana生图 → 美术指导审核 → 调整提示词 → 再生图
```
